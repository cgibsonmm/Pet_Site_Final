-# frozen_string_literal: true
= simple_form_for([@user, @animal]) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :dob
    = f.input :species

  .form-actions
    = f.button :submit
